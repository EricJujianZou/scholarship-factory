# Public site storefront config, read by `sf site`. Nothing here is secret:
# the IG handle and Stripe payment link are public by nature.

instagram = "sleppyeric"
price = "$15/mo"

stripe_url = "https://buy.stripe.com/5kQ6oGbB6bzS8gt0O21gs00"

# email capture stays off until a form backend exists:
# email_form_action = "https://..."

# sources the owner retired from the public page (2026-08-06); their rows
# stay in the DB but never reach an export or splice
retired_sources = ["opportunitydesk.org", "opportunitiesforyouth.org"]
